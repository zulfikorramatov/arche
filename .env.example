# App
APP_NAME=arche
APP_ENV=local

# HTTP
HTTP_ADDR=:8080
HTTP_READ_TIMEOUT=10s
HTTP_WRITE_TIMEOUT=10s
HTTP_SHUTDOWN_TIMEOUT=15s

# Logger
LOG_LEVEL=info
LOG_FORMAT=json
LOG_APM=false
LOG_LOG_SAMPLING_RATE=100

# Postgres (use "postgres" host inside docker; docker-compose overrides this
# for the app container — keep localhost here so `make run` works on the host)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app
POSTGRES_SSL_MODE=disable
POSTGRES_MAX_CONNS=10
POSTGRES_MIN_CONNS=1
POSTGRES_CONN_TIMEOUT=5s

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_POOL_SIZE=10
REDIS_DIAL_TIMEOUT=5s

# Elastic APM
ELASTIC_APM_SERVER_URL=http://localhost:8200
ELASTIC_APM_SERVICE_NAME=arche
ELASTIC_APM_ENVIRONMENT=local
ELASTIC_APM_SECRET_TOKEN=
